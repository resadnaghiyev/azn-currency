spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/currency_v6
    username: rashad
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: true

server:
  servlet:
    context-path: /api/v1

springdoc:
  swagger-ui:
    tagsSorter: alpha